<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!-- 映射文件，映射到对应的SQL接口 -->
<mapper namespace="example.demo.mapper.IUsersMapper">


    <!-- 查询数据 -->
    <!-- 入参定义：在接口定义中使用@Param注解(单参/多参都可使用) -->
    <!-- 语句末尾不能有分号：ORA-00911: invalid character -->
    <select id="findByClientId" resultType="example.demo.entity.Users">
        SELECT u.id, u.username, u.password FROM users u WHERE 1=1
        <if test="username != null">
            and t.username = #{username}
        </if>
        <if test="password != null">
            and t.password = #{password}
        </if>
        order by u.id
    </select>

</mapper>