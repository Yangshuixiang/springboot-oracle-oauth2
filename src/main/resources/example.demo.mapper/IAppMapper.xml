<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!-- 映射文件，映射到对应的SQL接口 -->
<mapper namespace="example.demo.mapper.IAppMapper">


    <!-- 查询数据 -->
    <!-- 入参定义：在接口定义中使用@Param注解(单参/多参都可使用) -->
    <!-- 语句末尾不能有分号：ORA-00911: invalid character -->
    <select id="findByClientId" resultMap="APP_resultMap" resultType="example.demo.entity.App">
        select t.id, t.client_id, t.client_secret, t.redirect_uri from app t where 1=1
        <if test="clientId != null">
            and t.client_id = #{clientId}
        </if>
        order by t.id
    </select>

</mapper>